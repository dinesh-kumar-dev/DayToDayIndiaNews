<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dineshpro.daytodayindia">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".DeleteAndUpdate" />
        <activity android:name=".DeleteUpdateDashbord" />
        <activity android:name=".NewsDetails" />
        <activity android:name=".NewsList" />
        <activity android:name=".CreateUser" />
        <activity android:name=".UserLogin" />
        <activity android:name=".AddNewsActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".DashBoard"
            android:label="@string/title_activity_dash_board"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".NotificationMessagingService">
            <intent-filter>
                <action android:name="com.google.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>